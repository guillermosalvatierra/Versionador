package main;

import java.lang.reflect.Constructor;

public class ObtenedorDeObjetoPorArchivo {


		
/*	public Object traerInformacionDeAtributos( String o,String atr ){
		
		Class<? extends Object> clase = null;
		Object obj=null;
		try {
			clase = Class.forName(o);
			Constructor constructor = clase.getConstructor(new Class[]{Integer.class, Integer.class,Integer.class});
			
			//Class[] argTypes = {int.class};
			Constructor constructor = curr.getDeclaredConstructor(argTypes);

			clase.getConstructor().getParameterTypes();
			
			Object[] arguments = {new Integer(5)};
			
			Object instance = constructor.newInstance(arguments);
			
			//Object objetoTimer =  constructor.newInstance(1, 1,1);
			
			obj=clase.isInstance(UtilidadesJson.obtenerAtributoConSeparador(atr));
			
			
		} catch (ClassNotFoundException | NoSuchMethodException | SecurityException e) {
			// TODO Auto-generated catch block
			e.printStackTrace();
		}
		
		return obj;
	}
	*/
}
